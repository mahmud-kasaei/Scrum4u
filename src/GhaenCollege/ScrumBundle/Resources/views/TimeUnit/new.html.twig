{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TimeUnit creation</h1>

    <form action="{{ path('time_unit_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('time_unit') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
