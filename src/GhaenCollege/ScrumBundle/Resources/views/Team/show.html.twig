{% extends "GhaenCollegeScrumBundle:Parent:administration_menu.html.twig" %}
{% block stylesheet %}

{% endblock %}
{% block title %}
    <title>مشاهده تیم  </title>
{% endblock %}
{% block container -%}
    <h1>تیم</h1>
    <div class="row-fluid">
        <div class="span12">
            <div class="box">
                <div class="box-container-toggle">
                    <div class="box-content">
                        <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered bootstrap-datatable" id="datatable">
                            <thead>
                            <tr>
                                <th class="centerclass">کد</th>
                                <th class="centerclass">عنوان</th>
                                <th class="centerclass">توضیحات</th>
                                <th class="centerclass"></th>
                            </tr>
                            </thead>
                            <tbody>
                            {#{% for entity in entities %}#}
                                <tr>
                                    <td>{{ entity.id }}</td>
                                    <td>{{ entity.title }}</td>
                                    <td>{{ entity.description }}</td>
                                    <td>
                                        <a class="btn btn-success" href="{{ path('team_show', { 'id': entity.id }) }}">مشاهده  <i class="icon-zoom-in icon-white"></i></a>
                                        <a class="btn btn-info" href="{{ path('team_edit', { 'id': entity.id }) }}">ویرایش      <i class="icon-edit icon-white"></i></a>
                                        {#<li>#}
                                            <form action="{{ path('team_delete', { 'id': entity.id }) }}" method="post">
                                                <input type="hidden" name="_method" value="DELETE" />
                                                {{ form_widget(delete_form) }}
                                                <button type="submit">حذف</button>
                                            </form>
                                        {#</li>#}
                                    </td>
                                </tr>
                            {#{% endfor %}#}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {#<table class="record_properties">#}
        {#<tbody>#}
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ entity.id }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Title</th>#}
                {#<td>{{ entity.title }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Description</th>#}
                {#<td>{{ entity.description }}</td>#}
            {#</tr>#}
        {#</tbody>#}
    {#</table>#}

        <ul class="record_actions">
    <li>
        <a href="{{ path('team') }}">
            برگشت به لیست
        </a>
    </li>

    {#<li>#}
        {#<form action="{{ path('team_delete', { 'id': entity.id }) }}" method="post">#}
            {#<input type="hidden" name="_method" value="DELETE" />#}
            {#{{ form_widget(delete_form) }}#}
            {#<button type="submit">حذف</button>#}
        {#</form>#}
    {#</li>#}
</ul>
{% endblock %}
